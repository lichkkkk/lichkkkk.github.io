---
layout: post
title: "Hello, World"
date: 2026-05-22 12:00:00 -0700
categories: meta
---

This is the first post on the blog. It exists mostly to prove the pipeline
works end to end: Markdown in `_posts/`, Jekyll on GitHub Pages, HTML out.

A few things to try next:

- Replace this post with something you actually wrote.
- Edit `_config.yml` to change the site title, description, and links.
- Add a new post by dropping a file into `_posts/` named
  `YYYY-MM-DD-some-slug.md` with a similar front-matter block.

## Code blocks work too

```python
def greet(name: str) -> str:
    return f"hello, {name}"
```

That's it. Welcome.
